面向对象，
封装，继承，多态


new:
1.创建一个空对象{}
2.this = 这个空对象
3.将this(这个空对象).__proto__ = 该函数.prototype
4.如果没有返回对象（没有return其他的对象），则返回this(这个空对象)，这里也很重要，因为__proto__是给空对象的，如果return了对象出去，那么，prototype就接收不到了


原型 prototype

css 

class: 一次给一组元素加样式
行间样式： 一次只给一个元素加样式


类，对象(对象是类制造出来的)

类是个模子           构造函数
对象是产品           实例                

prototype解决了一个问题，公共方法可以调用同一个函数，
工厂模式出来的，每次都会新创建一个方法，
但用prototype出来的，指向的都是同个方法
构造函数（类）命名头部大写


